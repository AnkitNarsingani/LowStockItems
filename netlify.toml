[build]
  # Migrations are NOT run here yet. Netlify does not expose build logs over
  # its API, so a migration failure during the build is invisible from outside
  # — and it takes the whole deploy with it, including the health endpoint that
  # would have explained why. Get /api/health green first, then move the
  # migration back in front of the build.
  command = "npm run build"
  publish = "build"
  functions = "netlify/functions"

# The API must be matched before the SPA catch-all below, or every /api/*
# request would be answered with index.html. Netlify evaluates redirects in
# file order, so this pass-through has to come first.
[[redirects]]
  from = "/api/*"
  to = "/api/:splat"
  status = 200
  force = false

# Makes BrowserRouter work on refresh and direct links (/po/new, /settings).
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
